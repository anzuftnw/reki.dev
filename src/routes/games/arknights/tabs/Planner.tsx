export function ArknightsPlannerTab() {
  return (
    <div class="flex flex-col gap-2 rounded-xl border border-dashed border-border p-6 text-center">
      <p class="text-sm text-text-2">Planner isn't built out yet.</p>
      <p class="text-sm text-text-3">
        This tab is intentionally a placeholder — farming/leveling math is real per-game logic (material costs,
        priority queues, etc.) that shouldn't be guessed. Let me know what you actually want tracked here.
      </p>
    </div>
  )
}
